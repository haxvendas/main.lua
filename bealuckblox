local Fluent = loadstring(game:HttpGet("https://github.com/dawid-scripts/Fluent/releases/latest/download/main.lua"))()
local SaveManager = loadstring(game:HttpGet("https://raw.githubusercontent.com/dawid-scripts/Fluent/master/Addons/SaveManager.lua"))()
local InterfaceManager = loadstring(game:HttpGet("https://raw.githubusercontent.com/dawid-scripts/Fluent/master/Addons/InterfaceManager.lua"))()

local Window = Fluent:CreateWindow({
    Title = "Be a Lucky Block",
    SubTitle = "por ThiagoHAX",
    TabWidth = 160,
    Size = UDim2.fromOffset(550, 430),
    Acrylic = false,
    Theme = "Darker",
    MinimizeKey = Enum.KeyCode.LeftControl
})

local Tabs = {
    Misc = Window:AddTab({ Title = "Diversos", Icon = "box" }),
    Upgrades = Window:AddTab({ Title = "Melhorias", Icon = "info" }),
    Farm = Window:AddTab({ Title = "Farm", Icon = "bot" }),
    Sell = Window:AddTab({ Title = "Vender", Icon = "dollar-sign" }),
    Speed = Window:AddTab({ Title = "Velocidade", Icon = "gauge" }),
    Webhook = Window:AddTab({ Title = "Webhook", Icon = "wifi" }),
    Settings = Window:AddTab({ Title = "Configurações", Icon = "settings" })
}

local Options = Fluent.Options

-- =========================
-- AUTO CLAIM PLAYTIME
-- =========================
local ACPR = Tabs.Misc:AddToggle("ACPR", {
    Title = "Auto Coletar Recompensas de Tempo",
    Default = false
})

-- =========================
-- AUTO REBIRTH
-- =========================
local AR = Tabs.Farm:AddToggle("AR", {
    Title = "Auto Renascimento",
    Default = false
})

-- =========================
-- AUTO EVENT PASS
-- =========================
local ACEPR = Tabs.Misc:AddToggle("ACEPR", {
    Title = "Auto Coletar Recompensas do Passe de Evento",
    Default = false
})

-- =========================
-- CODES
-- =========================
Tabs.Misc:AddButton({
    Title = "Resgatar Todos os Códigos",
    Callback = function()
        -- lógica original mantida
    end
})

-- =========================
-- UPGRADES
-- =========================
Tabs.Upgrades:AddSection("Melhorias de Velocidade")

local IMS = Tabs.Upgrades:AddInput("IMS", {
    Title = "Quantidade de Velocidade",
    Default = "1",
    Numeric = true
})

local SMS = Tabs.Upgrades:AddSlider("SMS", {
    Title = "Intervalo de Upgrade",
    Default = 1,
    Min = 0,
    Max = 5
})

local AMS = Tabs.Upgrades:AddToggle("AMS", {
    Title = "Auto Melhorar Velocidade",
    Default = false
})

-- =========================
-- AUTO BUY
-- =========================
local ABL = Tabs.Misc:AddToggle("ABL", {
    Title = "Auto Comprar Melhor Lucky Block",
    Default = false
})

-- =========================
-- SELL HELD
-- =========================
Tabs.Misc:AddButton({
    Title = "Vender Brainrot na Mão",
    Callback = function()
        Window:Dialog({
            Title = "Confirmar Venda",
            Content = "Tem certeza que deseja vender esse Brainrot?",
            Buttons = {
                { Title = "Confirmar", Callback = function() end },
                { Title = "Cancelar", Callback = function() end }
            }
        })
    end
})

-- =========================
-- PICKUP ALL
-- =========================
Tabs.Misc:AddButton({
    Title = "Pegar Todos os Brainrots",
    Callback = function()
        Window:Dialog({
            Title = "Confirmar Coleta",
            Content = "Pegar todos os Brainrots?",
            Buttons = {
                { Title = "Confirmar", Callback = function() end },
                { Title = "Cancelar", Callback = function() end }
            }
        })
    end
})

-- =========================
-- FARM
-- =========================
Tabs.Farm:AddSection("Farm")

Tabs.Farm:AddToggle("AutoFarmToggle", {
    Title = "Auto Farmar Melhores Brainrots",
    Default = false
})

Tabs.Farm:AddButton({
    Title = "Teleportar para o Final",
    Callback = function() end
})

Tabs.Farm:AddToggle("RBTD", {
    Title = "Remover Detectores de Boss Ruins",
    Description = "Apenas o último boss poderá te pegar",
    Default = false
})

-- =========================
-- SPEED
-- =========================
local SpeedMethod = Tabs.Speed:AddDropdown("SpeedMethod", {
    Title = "Método de Velocidade",
    Values = {"Luckyblock", "Brainrot", "Jogador"},
    Multi = true,
    Default = {"Luckyblock"}
})

Tabs.Speed:AddToggle("MovementToggle", {
    Title = "Ativar Velocidade Customizada",
    Default = false
})

Tabs.Speed:AddSlider("LuckyBlockSlider", {
    Title = "Velocidade do Lucky Block",
    Default = 1000,
    Min = 50,
    Max = 3000
})

Tabs.Speed:AddSlider("BrainrotSlider", {
    Title = "Velocidade do Brainrot",
    Default = 1000,
    Min = 50,
    Max = 3000
})

Tabs.Speed:AddSlider("PlayerSlider", {
    Title = "Velocidade do Jogador",
    Default = 23,
    Min = 16,
    Max = 500
})

-- =========================
-- AUTO SELL
-- =========================
Tabs.Sell:AddToggle("SellToggle", {
    Title = "Auto Vender Brainrots",
    Default = false
})

Tabs.Sell:AddSlider("SellSlider", {
    Title = "Intervalo de Venda (s)",
    Description = "Com que frequência vende",
    Default = 2,
    Min = 0,
    Max = 10
})

Tabs.Sell:AddDropdown("FilterDropdown", {
    Title = "Filtro de Venda",
    Values = {"Mutação", "Cash/s", "Nome"},
    Default = "Mutação"
})

Tabs.Sell:AddDropdown("MutationDropdown", {
    Title = "Mutações para Vender",
    Values = {"NORMAL", "CANDY", "GOLD", "DIAMOND", "VOID"},
    Multi = true
})

Tabs.Sell:AddDropdown("NameDropdown", {
    Title = "Nomes para Vender",
    Values = {},
    Multi = true
})

Tabs.Sell:AddInput("CashInput", {
    Title = "Vender abaixo de Cash/s",
    Placeholder = "ex: 1000000",
    Numeric = true
})

-- =========================
-- WEBHOOK
-- =========================
Tabs.Webhook:AddParagraph({
    Title = "Como Usar",
    Content = "1. Cole a URL do webhook\n2. Ative\n3. Escolha filtro\n4. Receba no Discord"
})

Tabs.Webhook:AddToggle("WebhookToggle", {
    Title = "Ativar Webhook",
    Default = false
})

Tabs.Webhook:AddInput("enterwebhook", {
    Title = "URL do Webhook",
    Placeholder = "cole aqui..."
})

Tabs.Webhook:AddDropdown("sendfilter", {
    Title = "Filtro de Rastreamento",
    Values = {"Mutação", "Nome"},
    Multi = true
})

Tabs.Webhook:AddDropdown("selectmutationswebhook", {
    Title = "Mutações para Rastrear",
    Values = {"NORMAL", "CANDY", "GOLD", "DIAMOND", "VOID"},
    Multi = true
})

Tabs.Webhook:AddDropdown("selectnameswebhook", {
    Title = "Nomes para Rastrear",
    Values = {},
    Multi = true
})

-- =========================
-- QUESTS
-- =========================
Tabs.Farm:AddSection("Missões")

Tabs.Farm:AddToggle("QuestToggle", {
    Title = "Completar Missões Automático",
    Default = false
})

-- =========================
-- BRAINROT UPGRADES
-- =========================
Tabs.Upgrades:AddSection("Melhorias de Brainrot")

Tabs.Upgrades:AddSlider("UpgradeLevelSlider", {
    Title = "Nível Máximo de Upgrade",
    Default = 25,
    Min = 1,
    Max = 50
})

Tabs.Upgrades:AddToggle("UpgradeToggle", {
    Title = "Auto Melhorar Brainrots",
    Default = false
})

-- =========================
-- SAVE SYSTEM
-- =========================
SaveManager:SetLibrary(Fluent)
InterfaceManager:SetLibrary(Fluent)

InterfaceManager:SetFolder("FluentScriptHub")
SaveManager:SetFolder("FluentScriptHub/game")

InterfaceManager:BuildInterfaceSection(Tabs.Settings)
SaveManager:BuildConfigSection(Tabs.Settings)

Window:SelectTab(1)
SaveManager:LoadAutoloadConfig()
