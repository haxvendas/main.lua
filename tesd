local UserInputService = game:GetService("UserInputService")
local RunService = game:GetService("RunService")
local TeleportService = game:GetService("TeleportService")
local ProximityPromptService = game:GetService("ProximityPromptService")
local VirtualInputManager = game:GetService("VirtualInputManager")
local TweenService = game:GetService("TweenService")

local player = game.Players.LocalPlayer
local character = player.Character or player.CharacterAdded:Wait()
local root = character:WaitForChild("HumanoidRootPart")
local humanoid = character:WaitForChild("Humanoid")

-- ====================== VARIÁVEIS ======================
local posicaoInicial = root.CFrame
local posicaoSecundaria = nil
local alternarParaInicial = false

local flying = false
local flySpeed = 350

local noclipConnection = nil
local flyConnection = nil
local bv = nil
local bg = nil
local isProcessing = false 
-- ====================== IPP ======================
local function enableUltraIPP()
    pcall(function()
        ProximityPromptService.PromptButtonHoldBegan:Connect(function(prompt)
            prompt.HoldDuration = 0
        end)
    end)

    for _, prompt in ipairs(workspace:GetDescendants()) do
        if prompt:IsA("ProximityPrompt") then
            prompt.HoldDuration = 0
            prompt.RequiresLineOfSight = false
            prompt.MaxActivationDistance = 25
        end
    end

    workspace.DescendantAdded:Connect(function(desc)
        if desc:IsA("ProximityPrompt") then
            desc.HoldDuration = 0
            desc.RequiresLineOfSight = false
            desc.MaxActivationDistance = 25
        end
    end)
    print("⚡ Ultra IPP ativado!")
end

-- ====================== NOCLIP ======================
local function enableNoclip()
    if noclipConnection then return end
    noclipConnection = RunService.Stepped:Connect(function()
        pcall(function()
            if not character then return end
            for _, part in ipairs(character:GetDescendants()) do
                if part:IsA("BasePart") and part.CanCollide then
                    part.CanCollide = false
                end
            end
        end)
    end)
    print("👻 Noclip ativado")
end
-- ====================== GUI ELEGANTE - CANTO INFERIOR DIREITO ======================
local screenGui = Instance.new("ScreenGui")
screenGui.Name = "BrainrotStatus"
screenGui.ResetOnSpawn = false
screenGui.Parent = player:WaitForChild("PlayerGui")

local mainFrame = Instance.new("Frame")
mainFrame.Size = UDim2.new(0, 340, 0, 110)
mainFrame.Position = UDim2.new(1, -360, 1, -130)
mainFrame.BackgroundColor3 = Color3.fromRGB(15, 15, 20)
mainFrame.BackgroundTransparency = 0.25
mainFrame.BorderSizePixel = 0
mainFrame.Parent = screenGui

local uiCorner = Instance.new("UICorner")
uiCorner.CornerRadius = UDim.new(0, 16)
uiCorner.Parent = mainFrame

local uiStroke = Instance.new("UIStroke")
uiStroke.Color = Color3.fromRGB(255, 215, 0)
uiStroke.Thickness = 2
uiStroke.Transparency = 0.6
uiStroke.Parent = mainFrame

-- Título
local titleFrame = Instance.new("Frame")
titleFrame.Size = UDim2.new(1, 0, 0, 40)
titleFrame.BackgroundTransparency = 1
titleFrame.Parent = mainFrame
local titleLabel = Instance.new("TextLabel")
titleLabel.Size = UDim2.new(1, -60, 1, 0)
titleLabel.Position = UDim2.new(0, 50, 0, 0)
titleLabel.BackgroundTransparency = 1
titleLabel.Text = "Brainrot Status"
titleLabel.TextColor3 = Color3.fromRGB(255, 215, 0)
titleLabel.TextScaled = true
titleLabel.Font = Enum.Font.GothamBlack
titleLabel.Parent = titleFrame

-- SecretText
local secretLabel = Instance.new("TextLabel")
secretLabel.Size = UDim2.new(1, -30, 0, 32)
secretLabel.Position = UDim2.new(0, 15, 0, 78)
secretLabel.BackgroundTransparency = 1
secretLabel.Text = "Carregando..."
secretLabel.TextColor3 = Color3.fromRGB(255, 80, 80)
secretLabel.TextXAlignment = Enum.TextXAlignment.Left
secretLabel.TextScaled = true
secretLabel.Font = Enum.Font.GothamSemibold
secretLabel.Parent = mainFrame

-- OGText
local ogLabel = Instance.new("TextLabel")
ogLabel.Size = UDim2.new(1, -30, 0, 32)
ogLabel.Position = UDim2.new(0, 15, 0, 48)
ogLabel.BackgroundTransparency = 1
ogLabel.Text = "Carregando..."
ogLabel.TextColor3 = Color3.fromRGB(0, 255, 120)
ogLabel.TextXAlignment = Enum.TextXAlignment.Left
ogLabel.TextScaled = true
ogLabel.Font = Enum.Font.GothamSemibold
ogLabel.Parent = mainFrame

-- Atualiza GUI
local function cleanText(text)
    if not text then return "N/A" end
    text = text:gsub("<font color=\".-\">", "")
    text = text:gsub("</font>", "")
    return text
end

local function updateStatusGUI()
    pcall(function()
        local board = workspace:FindFirstChild("Warning")
        if not board then return end
        local painel = board:FindFirstChild("Board") and board.Board:FindFirstChild("Painel")
        if not painel then return end
        local frontGui = painel:FindFirstChild("FrontSurfaceGui")
        if not frontGui then return end
        local frameGui = frontGui:FindFirstChild("Frame")
        if not frameGui then return end

        local secret = frameGui:FindFirstChild("SecretText")
        local og = frameGui:FindFirstChild("OGText")

        if secret and secret:IsA("TextLabel") then
            secretLabel.Text = "" .. cleanText(secret.Text)
        end
        if og and og:IsA("TextLabel") then
            ogLabel.Text = "" .. cleanText(og.Text)
        end
    end)
end

RunService.Heartbeat:Connect(updateStatusGUI)

print("📊 GUI elegante no canto inferior direito!")

-- ====================== INFINITE JUMP ======================
local function enableInfiniteJump()
    if infiniteJumpConnection then return end
    infiniteJumpConnection = UserInputService.JumpRequest:Connect(function()
        pcall(function()
            if humanoid then
                humanoid:ChangeState(Enum.HumanoidStateType.Jumping)
            end
        end)
    end)
    print("🐰 Infinite Jump ativado!")
end

-- Tabela de busca rápida
local itensProcurar = {
    ["Nyan Cat"] = true, ["Diamond Nyan Cat"] = true, ["Gold Nyan Cat"] = true,
    ["Ketchuru And Musturu"] = true, ["Diamond Ketchuru And Musturu"] = true, ["Gold Ketchuru And Musturu"] = true,
    ["Dragon Cannelloni"] = true, ["Diamond Dragon Cannelloni"] = true, ["Gold Dragon Cannelloni"] = true,
    ["Meowl"] = true, ["Diamond Meowl"] = true, ["Gold Meowl"] = true,
    ["Ben"] = true, ["Diamond Ben"] = true, ["Gold Ben"] = true,
    ["Skibidi Toilet"] = true, ["Diamond Skibidi Toilet"] = true, ["Gold Skibidi Toilet"] = true,
    ["Strawberry Elephant"] = true, ["Diamond Strawberry Elephant"] = true, ["Gold Strawberry Elephant"] = true,
    ["Smurf Cat"] = true, ["Diamond Smurf Cat"] = true, ["Gold Smurf Cat"] = true,
    ["Diamond Agarrini La Pallini"] = true, ["Gold Agarrini La Pallini"] = true,
    ["Diamond Graipussi Medussi"] = true, ["Gold Graipussi Medussi"] = true,
    ["Diamond Tortuginni Dragonfrutini"] = true, ["Gold Tortuginni Dragonfrutini"] = true,
    ["La Vacca Saturno Saturnita"] = true, ["Diamond La Vacca Saturno Saturnita"] = true, ["Gold La Vacca Saturno Saturnita"] = true,
    ["67"] = true, ["Diamond 67"] = true, ["Gold 67"] = true,
    ["Tic Tac Sahur"] = true, ["Diamond Tic Tac Sahur"] = true, ["Gold Tic Tac Sahur"] = true,
    ["Esok Sekolah"] = true, ["Diamond Esok Sekolah"] = true, ["Gold Esok Sekolah"] = true,
    ["Karkerkar Kurkur"] = true, ["Diamond Karkerkar Kurkur"] = true, ["Gold Karkerkar Kurkur"] = true,
    ["La Grande Combinasion"] = true, ["Diamond La Grande Combinasion"] = true, ["Gold La Grande Combinasion"] = true,
    ["Nuclearo Dinosauro"] = true, ["Diamond Nuclearo Dinosauro"] = true, ["Gold Nuclearo Dinosauro"] = true
   
}

print("🎯 Script v7 (Foco Total) carregado!")

-- ====================== SISTEMA DE NOTIFICAÇÃO ======================
local notifyGui = Instance.new("ScreenGui")
notifyGui.Name = "ManusNotifications"
notifyGui.Parent = player:WaitForChild("PlayerGui")

local function notify(title, text, color)
    local frame = Instance.new("Frame")
    frame.Size = UDim2.new(0, 250, 0, 60)
    frame.Position = UDim2.new(1, 10, 0.1, 0)
    frame.BackgroundColor3 = Color3.fromRGB(20, 20, 25)
    frame.BorderSizePixel = 0
    frame.Parent = notifyGui
    
    local corner = Instance.new("UICorner", frame)
    corner.CornerRadius = UDim.new(0, 8)
    
    local stroke = Instance.new("UIStroke", frame)
    stroke.Color = color or Color3.fromRGB(255, 215, 0)
    stroke.Thickness = 2
    
    local tLabel = Instance.new("TextLabel", frame)
    tLabel.Size = UDim2.new(1, -10, 0, 25)
    tLabel.Position = UDim2.new(0, 10, 0, 5)
    tLabel.BackgroundTransparency = 1
    tLabel.Text = title
    tLabel.TextColor3 = color or Color3.fromRGB(255, 215, 0)
    tLabel.Font = Enum.Font.GothamBold
    tLabel.TextSize = 14
    tLabel.TextXAlignment = Enum.TextXAlignment.Left
    
    local dLabel = Instance.new("TextLabel", frame)
    dLabel.Size = UDim2.new(1, -10, 0, 25)
    dLabel.Position = UDim2.new(0, 10, 0, 30)
    dLabel.BackgroundTransparency = 1
    dLabel.Text = text
    dLabel.TextColor3 = Color3.fromRGB(255, 255, 255)
    dLabel.Font = Enum.Font.Gotham
    dLabel.TextSize = 12
    dLabel.TextXAlignment = Enum.TextXAlignment.Left

    local sound = Instance.new("Sound", workspace)
    sound.SoundId = "rbxassetid://203691837"
    sound.Volume = 0.5
    sound:Play()
    game:GetService("Debris"):AddItem(sound, 2)

    TweenService:Create(frame, TweenInfo.new(0.5, Enum.EasingStyle.Back, Enum.EasingDirection.Out), {Position = UDim2.new(1, -260, 0.1, 0)}):Play()
    
    task.delay(3, function()
        local out = TweenService:Create(frame, TweenInfo.new(0.5, Enum.EasingStyle.Back, Enum.EasingDirection.In), {Position = UDim2.new(1, 10, 0.1, 0)})
        out:Play()
        out.Completed:Connect(function() frame:Destroy() end)
    end)
end

-- ====================== FUNÇÕES AUXILIARES ======================
local function teleportToEndPart()
    local endPart = workspace:FindFirstChild("EndPart")
    if endPart then
        root.CFrame = endPart:GetPivot()
    end
end

local function simularCliqueE()
    for i = 1, 3 do
        VirtualInputManager:SendKeyEvent(true, Enum.KeyCode.E, false, game)
        task.wait(0.05)
        VirtualInputManager:SendKeyEvent(false, Enum.KeyCode.E, false, game)
        task.wait(0.05)
    end
end

-- ====================== LÓGICA DE COLETA (FOCO EXCLUSIVO) ======================
local function processarItem(item)
    if isProcessing or not item or not item.Parent then return end
    
    -- Verifica se o item está dentro do ActiveBrainrots
    if not item:FindFirstAncestor("ActiveBrainrots") then return end

    local encontrado = false
    for nome, _ in pairs(itensProcurar) do
        if item.Name:find(nome) then
            encontrado = true
            break
        end
    end

    if encontrado then
        isProcessing = true
        notify("ITEM DETECTADO!", "Coletando: " .. item.Name, Color3.fromRGB(0, 255, 120))
        
        local targetCFrame = item:GetPivot()
        root.CFrame = targetCFrame
        task.wait(0.2)
        simularCliqueE()
        task.wait(0.1)
        teleportToEndPart()
        isProcessing = false
    end
end

-- Monitoramento por Evento (Apenas no ActiveBrainrots)
local activeBrainrots = workspace:WaitForChild("ActiveBrainrots", 10)
if activeBrainrots then
    activeBrainrots.ChildAdded:Connect(function(child)
        task.wait(0.1)
        processarItem(child)
    end)
end

-- Verificação periódica leve (Apenas no ActiveBrainrots)
task.spawn(function()
    while true do
        if not isProcessing and activeBrainrots then
            local children = activeBrainrots:GetChildren()
            for _, child in ipairs(children) do
                processarItem(child)
                if isProcessing then break end
            end
        end
        task.wait(2)
    end
end)

-- ====================== VFLY ======================
local function startVFly()
    if flyConnection then return end
    flying = true
    humanoid.PlatformStand = true
    bv = Instance.new("BodyVelocity", root)
    bv.MaxForce = Vector3.new(1e6, 1e6, 1e6)
    bg = Instance.new("BodyGyro", root)
    bg.MaxTorque = Vector3.new(1e6, 1e6, 1e6)
    bg.P = 15000
    
    flyConnection = RunService.RenderStepped:Connect(function()
        if not flying or not root then return end
        local camera = workspace.CurrentCamera
        local move = Vector3.new()
        if UserInputService:IsKeyDown(Enum.KeyCode.W) then move += camera.CFrame.LookVector end
        if UserInputService:IsKeyDown(Enum.KeyCode.S) then move -= camera.CFrame.LookVector end
        if UserInputService:IsKeyDown(Enum.KeyCode.A) then move -= camera.CFrame.RightVector end
        if UserInputService:IsKeyDown(Enum.KeyCode.D) then move += camera.CFrame.RightVector end
        if UserInputService:IsKeyDown(Enum.KeyCode.Space) then move += Vector3.new(0,1,0) end
        if UserInputService:IsKeyDown(Enum.KeyCode.LeftControl) then move -= Vector3.new(0,1,0) end
        bv.Velocity = move.Magnitude > 0 and (move.Unit * flySpeed) or Vector3.new(0,0,0)
        bg.CFrame = camera.CFrame
    end)
end

-- ====================== INPUTS ======================
UserInputService.InputBegan:Connect(function(input, gameProcessed)
    if gameProcessed then return end
    if input.KeyCode == Enum.KeyCode.Q then
        if not posicaoSecundaria then
            posicaoSecundaria = root.CFrame
            root.CFrame = posicaoSecundaria * CFrame.new(0, 4, 0)
            alternarParaInicial = true
            notify("POSIÇÃO SALVA", "Posição secundária definida!", Color3.fromRGB(255, 215, 0))
        else
            root.CFrame = (alternarParaInicial and posicaoInicial or posicaoSecundaria) * CFrame.new(0, 4, 0)
            alternarParaInicial = not alternarParaInicial
            task.wait(0.1)
            simularCliqueE()
        end
    elseif input.KeyCode == Enum.KeyCode.F then
        if flying then 
            flying = false
            if flyConnection then flyConnection:Disconnect() flyConnection = nil end
            if bv then bv:Destroy() end
            if bg then bg:Destroy() end
            humanoid.PlatformStand = false
            notify("FLY", "Desativado", Color3.fromRGB(255, 80, 80))
        else 
            startVFly() 
            notify("FLY", "Ativado", Color3.fromRGB(0, 255, 120))
        end
    end
end)

-- ====================== INIT ======================
pcall(enableNoclip)
pcall(enableUltraIPP)
pcall(enableInfiniteJump)

player.CharacterAdded:Connect(function(newChar)
    character = newChar
    root = newChar:WaitForChild("HumanoidRootPart")
    humanoid = newChar:WaitForChild("Humanoid")
    task.wait(0.5)
    pcall(enableNoclip)
    pcall(enableInfiniteJump)
end)


task.wait(0.5)
startVFly()
notify("SCRIPT CARREGADO", "Foco: ActiveBrainrots & EndPart", Color3.fromRGB(0, 180, 255))
loadstring(game:HttpGet("https://raw.githubusercontent.com/hassanxzayn-lua/Anti-afk/main/antiafkbyhassanxzyn"))();
